apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: service-invoice
spec:
  filterTags:
    pattern: "^[0-9.]+$"
  imageRepositoryRef:
    name: service-invoice
  policy:
    numerical:
      order: "asc"
  #  semver:
  #    range: 5.0.x
